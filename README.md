Even with such a primitive search implementation request takes on average 7 ms

To run the application

1. mvn clean package
2. Go to jar package
3. java -jar <jarName>
4. curl --location --request GET 'http://localhost:8080/search/Canon'